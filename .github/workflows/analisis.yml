name: IC Migue

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r req.txt
          pip install pytest pytest-cov flake8

      - name: Lint con flake8
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Ejecutar tests con cobertura
        run: |
          pytest pruebas/ --cov=pruebas --cov-report=xml

      - name: Codacy Analysis
        uses: codacy/codacy-analysis-cli-action@v4
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: success()
    outputs:
      app_url: ${{ steps.set_url.outputs.url }}
    steps:
      - uses: actions/checkout@v4

      - name: Deploy a Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{ secrets.SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}

      - name: Set app URL
        id: set_url
        run: echo "url=https://ic-ingsw.onrender.com/" >> $GITHUB_OUTPUT

  notify-slack-success:
    needs: deploy
    if: success()
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a Slack si fue exitoso 🎉
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{
            "text":"✅ *Deploy exitoso!* 🎉\nRepositorio: '${{ github.repository }}'\nAutor: '${{ github.actor }}'\n🔗 URL: https://ic-ingsw.onrender.com/",
            "attachments":[{"image_url":"https://media1.tenor.com/m/wsChytFfrS4AAAAd/monki-flip-monkey.gif","text":"¡Mirá qué bonito quedó!"}]
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}

  notify-slack-on-failure:
    needs: build
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a Slack si falló
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"🚨 *El workflow falló.*\nRepositorio: '${{ github.repository }}'\nCommit: '${{ github.sha }}'\nAutor: '${{ github.actor }}'"}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
